# دليل استكشاف مشكلة الصوت

## إذا لم ينتقل الصوت بعد التحديث الجديد

### الخطوة 1: فحص الـ Logs

افتح console/terminal وابحث عن هذه الرسائل:

**✅ إشارات النجاح:**
```
✅✅✅ CONFERENCE JOINED ✅✅✅
👤👤👤 NEW PARTICIPANT JOINED! 👤👤👤
🎤 Audio muted status: UNMUTED 🔊
```

**❌ إشارات المشاكل:**
```
❌❌❌ ERROR JOINING CALL ❌❌❌
❌ Error: [تفاصيل الخطأ]
```

---

### الخطوة 2: التحقق من الأذونات

#### Android:
تأكد أن `AndroidManifest.xml` يحتوي على:
```xml
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.INTERNET" />
```

#### iOS:
تأكد أن `Info.plist` يحتوي على:
```xml
<key>NSMicrophoneUsageDescription</key>
<string>نحتاج للميكروفون للمكالمات الصوتية</string>
<key>NSCameraUsageDescription</key>
<string>نحتاج للكاميرا للمكالمات المرئية</string>
```

---

### الخطوة 3: اختبار بسيط

**اختبار 1: نفس الغرفة في متصفحين**
1. افتح التطبيق على جهاز
2. افتح https://meet.jit.si/[room_name] في متصفح
3. استخدم نفس `room_name` الذي يظهر في console
4. تحدث في أحدهما واستمع في الآخر

**اختبار 2: جهازين موبايل**
1. افتح التطبيق على جهازين
2. انضم لنفس الغرفة
3. تأكد أن الميكروفون غير مكتوم (🔊)

---

### الخطوة 4: الأسباب المحتملة

#### السبب 1: الميكروفون مكتوم
**الحل:** اضغط على زر الميكروفون في التطبيق

#### السبب 2: إذن الميكروفون مرفوض
**الحل:** اذهب للإعدادات → الأذونات → فعّل الميكروفون

#### السبب 3: مشكلة في الشبكة/Firewall
**الحل:** جرب على شبكة واي فاي أخرى أو 4G/5G

#### السبب 4: Jitsi Meet SDK غير مثبت بشكل صحيح
**الحل:** 
```bash
flutter clean
flutter pub get
flutter run
```

---

### الخطوة 5: حل BONUS - استخدام Agora SDK

إذا استمرت المشكلة، يمكن استبدال Jitsi بـ Agora:

1. سجل في https://www.agora.io
2. احصل على App ID
3. أضف agora_rtc_engine في pubspec.yaml
4. أخبرني وسأقوم بتعديل الكود

---

## معلومات التشخيص

- Platform: Flutter
- Audio SDK: Jitsi Meet Flutter SDK
- Server: meet.jit.si
- Config: Simple & Compatible

## الدعم

إذا رأيت في logs:
- `CONFERENCE JOINED` ولكن لا يوجد `NEW PARTICIPANT` → المستخدم الآخر لم ينضم أو في غرفة مختلفة
- `NEW PARTICIPANT` موجود لكن لا صوت → مشكلة في جودة الاتصال أو firewall
- لا يوجد `CONFERENCE JOINED` → مشكلة في الاتصال بـ Jitsi server
