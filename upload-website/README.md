# موقع رفع الملفات - File Upload Website 🚀

موقع كامل لرفع ومشاركة الملفات حتى 150 ميجابايت مع backend server حقيقي.

## البنية 📁

```
upload-website/
├── client/              # Frontend (HTML, CSS, JS)
│   ├── index.html
│   ├── style.css
│   └── script.js
└── server/              # Backend (Node.js + Express)
    ├── server.js
    ├── package.json
    ├── uploads/         # مجلد الملفات المرفوعة
    └── files-db.json    # قاعدة البيانات (يتم إنشاؤها تلقائياً)
```

## التشغيل السريع 🚀

### 1. تثبيت Node.js
تأكد من تثبيت Node.js على جهازك:
- تحميل من: [nodejs.org](https://nodejs.org/)
- أو تحقق من التثبيت: `node --version`

### 2. تثبيت Dependencies
```bash
cd server
npm install
```

### 3. تشغيل السيرفر
```bash
npm start
```

أو للتطوير مع Auto-restart:
```bash
npm run dev
```

### 4. فتح الموقع
افتح المتصفح على:
```
http://localhost:3000
```

✅ الموقع يعمل الآن!

---

## المميزات ✨

### Backend Server
- ✅ Node.js + Express
- ✅ رفع ملفات حتى **150 ميجابايت**
- ✅ حفظ الملفات على السيرفر
- ✅ قاعدة بيانات JSON بسيطة
- ✅ API كامل للإدارة
- ✅ CORS enabled

### Frontend
- ✅ Drag & Drop للملفات
- ✅ Progress bar حقيقي
- ✅ تصميم Dark theme حديث
- ✅ واجهة عربية كاملة (RTL)
- ✅ Responsive لجميع الأجهزة

### الوظائف
- ✅ رفع ملفات
- ✅ تحميل ملفات
- ✅ حذف ملفات
- ✅ نسخ رابط التحميل
- ✅ عرض معلومات الملف

---

## API Endpoints 🔌

### Upload File
```
POST /api/upload
Content-Type: multipart/form-data
Body: file (FormData)
```

### Get All Files
```
GET /api/files
Response: { files: [...] }
```

### Download File
```
GET /api/download/:id
```

### Delete File
```
DELETE /api/files/:id
```

### Get File Info
```
GET /api/files/:id
```

---

## النشر على الإنترنت 🌐

### الخيار 1: Render (مجاني)
1. سجل في [render.com](https://render.com)
2. اربط مع GitHub repository
3. اختر "Web Service"
4. Build Command: `cd server && npm install`
5. Start Command: `cd server && npm start`

### الخيار 2: Railway (مجاني)
1. سجل في [railway.app](https://railway.app)
2. ارفع المشروع
3. سيتم نشره تلقائياً

### الخيار 3: Heroku
```bash
heroku create your-app-name
git push heroku main
```

---

## التطوير المستقبلي 🔮

### قاعدة بيانات حقيقية
استبدال `files-db.json` بـ:
- MongoDB
- PostgreSQL
- MySQL

### Cloud Storage
نقل الملفات من السيرفر إلى:
- AWS S3
- Google Cloud Storage
- Cloudinary

### مميزات إضافية
- [ ] نظام مستخدمين
- [ ] مجلدات وتصنيفات
- [ ] مشاركة خاصة بكلمة مرور
- [ ] تاريخ انتهاء للملفات
- [ ] معاينة الصور والفيديو

---

## الأمان 🔒

### الحماية الحالية
- حد أقصى 150MB لكل ملف
- CORS enabled
- Error handling شامل

### للإنتاج (ضروري)
- [ ] إضافة Authentication
- [ ] تحديد أنواع الملفات المسموحة
- [ ] فحص الملفات من الفيروسات
- [ ] Rate limiting
- [ ] HTTPS

---

## حل المشاكل 🔧

### المنفذ مشغول
```bash
# تغيير المنفذ
PORT=3001 npm start
```

### خطأ في رفع ملف كبير
تحقق من إعدادات `multer` في `server.js`:
```javascript
limits: {
    fileSize: 150 * 1024 * 1024 // 150MB
}
```

### مشكلة في CORS
تأكد من:
```javascript
app.use(cors());
```

---

## الترخيص 📄
هذا المشروع مفتوح المصدر ومجاني للاستخدام.

---

Made with ❤️ for easy file sharing
