# 🔔 دليل إعداد OneSignal - خطوة بخطوة

## ✅ ما تم إنجازه:
1. ✅ إضافة مكتبة OneSignal
2. ✅ إنشاء خدمة OneSignal
3. ✅ تحديث لوحة التحكم للإرسال
4. ✅ تهيئة في main.dart

---

## 📋 خطوات التفعيل (5-10 دقائق):

### الخطوة 1: التسجيل في OneSignal

1. **اذهب إلى**: [https://onesignal.com](https://onesignal.com)
2. **سجل حساب مجاني** (مجاني حتى 10,000 مستخدم)
3. انقر على **"New Organization/App"**
4. اختر **"Mobile App"**
5. أدخل اسم التطبيق (مثل: "تطبيقي")

### الخطوة 2: إعداد Android

1. اختر **Android** كمنصة
2. سيطلب منك **Firebase Server Key** (اختياري - يمكن تخطيه)
   - إذا تريد: اذهب لـ Firebase Console → Project Settings → Cloud Messaging → Server Key
   - أو: انقر **"Skip"** واستخدم OneSignal إعداداته
3. **سجّل اسم الحزمة**: `com.example.test7` (أو اسم package حزمتك من `android/app/build.gradle`)

### الخطوة 3: الحصول على المفاتيح

بعد الإعداد، ستحصل على:
- ✅ **App ID**: مثل `12345678-abcd-1234-abcd-123456789abc`
- ✅ **REST API Key**: مثل `YmFzaWMgxxxxxxxxxxxxxxxxx`

### الخطوة 4: إضافة المفاتيح للكود

#### أ) في `lib/services/onesignal_service.dart`:
```dart
static const String _appId = 'YOUR_ONESIGNAL_APP_ID'; // ضع App ID هنا
```

#### ب) في `lib/screens/notifications_tab.dart`:
```dart
const oneSignalAppId = 'YOUR_ONESIGNAL_APP_ID';  // نفس App ID
const oneSignalRestApiKey = 'YOUR_ONESIGNAL_REST_API_KEY';  // REST API Key
```

### الخطوة 5: إعداد Android Manifest

في `android/app/src/main/AndroidManifest.xml`، أضف **قبل** `<application`:

```xml
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
```

### الخطوة 6: تشغيل flutter pub get

```bash
flutter pub get
```

---

## 🧪 اختبار النظام:

### 1. تشغيل التطبيق:
```bash
flutter run
```

### 2. افتح لوحة التحكم (Admin Panel)
- اذهب لـ **"الإشعارات"**
- اكتب عنوان ورسالة
- انقر **"إرسال لجميع المستخدمين"**

### 3. سترى:
- ✅ الإشعار **محفوظ** في قاعدة البيانات
- ✅ Push Notification **حقيقي** يظهر في notification bar
- ✅ حتى لو التطبيق **مغلق**!

---

## 🎯 كيف يعمل النظام:

### طريقتان متكاملتان:

#### 1. Supabase (مضمون 100%):
- ✅ يحفظ الإشعار في `user_notifications`
- ✅ **لن يضيع أبداً**
- ✅ يظهر في التطبيق

#### 2. OneSignal (Push Notification):
- ✅ يرسل إشعار حقيقي في notification bar
- ✅ يظهر حتى لو التطبيق **مغلق**
- ✅ صوت + اهتزاز

### النتيجة:
- **الأفضل من الطريقتين!** 🎉
- إشعارات حقيقية + حفظ مضمون

---

## ❓ استكشاف الأخطاء:

### المشكلة: الإشعار لا يظهر والتطبيق مغلق
**الحل:**
1. تأكد من إضافة **App ID** و **REST API Key** الصحيحين
2. تأكد من إضافة permission في `AndroidManifest.xml`
3. جرب من جهاز حقيقي (ليس emulator)
4. افتح Settings → Apps → التطبيق → اسمح بالإشعارات

### المشكلة: لا أجد REST API Key
**الحل:**
1. OneSignal Dashboard → Settings
2. Keys & IDs → REST API Key (انسخه)

### المشكلة: يرسل في Supabase لكن ليس OneSignal
**الحل:**
- طبيعي! OneSignal يحتاج أجهزة مشتركة
- جرب من تطبيق حقيقي على جهاز

---

## 📊 مثال على النتيجة:

```
الأدمن يكتب: "تحديث جديد متاح!"
↓
1. يُحفظ في Supabase ✅
2. يُرسل عبر OneSignal ✅
↓
المستخدم (والتطبيق مغلق):
📱 تظهر notification في الهاتف!
↓
المستخدم يفتح التطبيق:
🔔 Badge أحمر (5+)
↓
يضغط على الإشعارات:
📋 يرى جميع الإشعارات المحفوظة
```

---

## 🎉 تهانينا!

الآن لديك نظام إشعارات **احترافي** يجمع:
- ✅ حفظ مضمون (Supabase)
- ✅ Push notifications حقيقية (OneSignal)
- ✅ سهولة الإعداد
- ✅ **مجاني 100%**

**أفضل من Firebase وأسهل!** 🚀
